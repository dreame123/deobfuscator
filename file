1544756099461283920
https://limewire.com/d/hjYdt#EZKasMAnMA
https://www.dropbox.com/t/lgbpyiaP91C3QauE
google/diffusiongemma-26b-a4b-it
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
https://github.com/ScriptTiger/LuaJIT-For-Windows/releases/latest 
https://aka.ms/vs/17/release/vc_redist.x64.exe
channel 1545833070542725201
prem acces 1546141138103050361
owner 1106911816900157552
out.write_text(result, encoding='utf-8')



# Dynamically find LuaJIT in the user's AppData folder (works for any username)
local_app_data = os.environ.get("LOCALAPPDATA")
if local_app_data:
    luajit_path = pathlib.Path(local_app_data) / "Programs" / "LuaJIT" / "bin" / "luajit.exe"
    if luajit_path.exists():
        LUAJIT = str(luajit_path)
    else:
        # Fallback to the bot's root folder
        LUAJIT = str(ROOT / "luajit.exe")
else:
    # Fallback to the bot's root folder
    LUAJIT = str(ROOT / "luajit.exe")
